The cloc Report was done by running `cloc .` in the `src` directory and in the `orgi` directory.

The the delta of the original state and the current state was calculated in the `clocReport.xlsx` Excel sheet.
